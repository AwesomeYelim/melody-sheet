# 서버
fastapi==0.104.1
uvicorn==0.29.0
pydantic==2.5.3
python-multipart==0.0.9

# 악보 처리
music21==8.3.0

# 오디오 처리
numpy==1.26.4
soundfile==0.12.1
librosa>=0.10.0
scipy>=1.11.0

# 딥러닝 AMT 파이프라인
# torch: 별도 설치 필요 (MPS GPU: pip install torch torchvision torchaudio)
basic-pitch>=0.3.0     # Spotify 피치 추출 (경량 CNN 기반)
noisereduce>=3.0.0     # 배경 노이즈 제거 (Whisper 갭 보정용)
pretty_midi>=0.2.10    # MIDI 생성

# 가사 추출
whisperx>=3.1.0        # WhisperX 음성→텍스트 + 강제정렬 (한국어 지원)

# 코드 감지
autochord>=0.1.0       # Bi-LSTM-CRF 오디오 코드 인식 (ISMIR 2021)
